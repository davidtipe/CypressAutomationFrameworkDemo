{
  "name": "cypressframeworkskilldemo",
  "version": "1.0.0",
  "description": "cypress automation skill demo",
  "main": "index.js",
  "scripts": {
    "cypress:open:demoqa": "cypress open --env configFile=demoqa",
    "cypress:run:demoqa": "cypress run --spec cypress/e2e/demoQA --browser chrome --headless --env configFile=demoqa",
    "cypress:open:restfulBooker": "cypress open --env configFile=restfulBooker",
    "cypress:run:restfulBooker": "cypress run --spec cypress/e2e/restfulBooker --browser chrome --headless --env configFile=restfulBooker"
  },
  "keywords": [
    "testing",
    "automation",
    "cypress",
    "framework"
  ],
  "author": "David Adetipe",
  "license": "ISC",
  "dependencies": {
    "cypress": "^12.11.0",
    "dotenv": "^16.4.7",
    "nodemailer": "^6.9.9"
  },
  "devDependencies": {
    "eslint-plugin-cypress": "^2.12.1"
  }
}
